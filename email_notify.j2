<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Software update notification</title>
    <style>
        html, body, th, td, p {
            font-family: Arial, sans-serif;
            font-size: 12px;
        }

        body {
            line-height: 1.6;
            color: #333;
        }

        h1 {
            color: #2c3e50;
            font-size: 18px;
        }

        p {
            margin: 10px 0;
        }

        table {
            border-collapse: collapse;
            width: 100%;
            max-width: 600px;
            margin: 20px 0;
        }

        th, td {
            padding: 10px;
            text-align: left;
            border: 1px solid #ddd;
        }

        th {
            background-color: #f5f5f5;
            font-weight: bold;
        }

        tr:nth-child(even) {
            background-color: #fafafa;
        }

        .footer {
            font-size: 12px;
            color: #777;
            margin-top: 20px;
        }
    </style>
</head>
<body>
<h1>Software update notification</h1>
<p>Hello, dear user!</p>
<p>The following is the latest software version update information, please refer to:</p>

<table>
    <thead>
    <tr>
        <th>Name</th>
        <th>Latest</th>
        <th>Previous</th>
    </tr>
    </thead>
    <tbody>
    {% for item in items %}
        <tr>
            <td>{{ item.name }}</td>
            <td>{{ item.latest }}</td>
            <td>{{ item.previous }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<p>If you have any questions, please feel free to contact us.</p>
<p>Good wishes,</p>
<p>L.STONE</p>
<div class="footer">
    <p>This email is automatically sent by the system, please do not reply.</p>
</div>
</body>
</html>